-- REM TODO: make whitespaces before lineNr possible
-- REM ToDO: Klammerung!!!

30 print "Zurück in die Zukunft!!"

80 input l$                                     REM -- TODO: left !!
-- 90 REM l$ = left$(q$, 1)                                    REM -- TODO: left !!
100 if (l$ = "J") or (l$ = "j") then goto 30

